apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: apps
  name: order-service
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8085"
    spec:
      containers:
      - name: order-service
        image: sonyms2023/order-service:2
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
        envFrom:
        - configMapRef:
            name: shop-config
        - secretRef:
            name: shop-secrets
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "k8s"
---
apiVersion: v1
kind: Service
metadata:
  namespace: apps
  name: order-service
spec:
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
